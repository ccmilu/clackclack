# 运行时产物
**/events.log
**/bridge.log
**/bridge.stdout.log
**/bridge.stderr.log

# daemon 生成的 helper .app（osacompile 编译产物，每台机器首次启动时自动生成）
**/KeyReturn.app/

# 自动生成的 Arduino 头文件（由 build_h.py 从 build/crab_data.py 转出）
Arduino/claude_status/crab_data.h

# 构建产物（资产管线生成的像素数据 + 调试 PNG，由 build_assets.py 重新生成）
build/

# CAD 生成的 STL（由 cad/case_v1.py 生成）
cad/*.stl

# 本机自用脚本（含本人 gh 操作 / 录制工具等，不入 git）
scripts/


/.claude/
/CLAUDE.md
/docs/
__pycache__/
*.pyc
.venv/
.DS_Store
.vscode/
.idea/
